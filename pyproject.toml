[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "grantdecay"
version = "1.0.3"
description = "Find privilege that was granted and never used."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "GrantDecay contributors" }]
keywords = ["security", "access-review", "least-privilege", "entitlements"]
classifiers = [
